server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  # local only — dışa açma, güvenlik kuralı
  location /api/ { proxy_pass http://dashboard-api:8001/api/; proxy_set_header Host $host; }
  location / { try_files $uri /index.html; }
}
