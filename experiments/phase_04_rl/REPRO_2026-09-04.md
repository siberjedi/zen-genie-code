# Phase 4.6 Reproducibility Fix — 2026-09-04 (fix + probe, full tuning YOK)

## Uygulanan fix'ler (R1/R2, protokol-dışı)
- `src/rl/determinism.py` (yeni): thread kilidi, warn_only deterministik mod,
  canonical hash, artifact save/load, runtime snapshot.
- `seed_all`: thread+deterministik modu çağırır (davranış değişmez).
- `rollout_trades`: reward dizisi de döndürür (6-tuple); `run_one` her koşuda
  `artifacts_<config>_<seed>.json` (action/equity/reward hash) + runtime snapshot yazar.
- `hash_model_weights`: container-safe ağırlık hash'i (dict/list recurse;
  pointer-hash tuzağı belgelendi).

## Probe (1 config C0-tabanı, seed42 ×2, 20,480 step, chunked+normalized)
- A: 43.8sn, 83 trade | B: 42.3sn, 83 trade
- model/action/equity/reward/trades/bounds/val-range: 7/7 AYNI → REPRO PASS.
- İlk FAIL'in kökü: smoke/probe inline hasher `np.ascontiguousarray(list_of_dicts)`
  ile OBJECT dizisi pointer hash'liyordu (ölçüm bug'ı, eğitim değil).
  Kanıt: construction-seviyesi testte policy tensor'ları identikti; pointer-hash
  farklıydı. `test_11_model_hash_container_safe` eklendi.
- PYTHONHASHSEED notu: süreç-içi setdefault etkisizdir (interpreter-startup okur);
  launcher-seviyesi pin önerilir, kritik yollarda `sorted()` mevcut.

## Kalan limitasyon (dürüst kayıt)
- Bit-eşitlik bu kısa probeda doğrulandı; 600k-step full koşular için garanti
  değil (gözlemlenmedi, iddia edilmedi). Full tuning'de artifact hash'leri
  koşu-başı karşılaştırma imkanı verir.
- `torch.use_deterministic_algorithms(warn_only=True)`: uyarı görülmedi;
  gelecekte uyarı çıkarsa raporlanacak (sessiz geçiştirme yok).

## Testler
- 11/11 (phase44) + 12/12 (hardened) PASS. Final B'ye temas yok.
