# Zen-Genie — Tam Durum Raporu — 2026-09-04 05:50 (UTC+3)

> **Kapsam:** `PROTOCOL.md:1` (LOCKED 2026-08-27) + `config/experiment.yaml:1` + `docs/CAPITAL_PROTECTION_EXECUTION_DESIGN.md:1` fiilen repo'da ne var, ne çalışıyor, ne eksik, deney kurallarına uyum.
> **Tarama:** `freqtrade/user_data/tradesv3.dryrun.sqlite` (114688b, 19 trade), `data/capital_state.json`, `dashboard/backend/app.py:1` (48533b, 17 endpoint), `dashboard/frontend/src/App.tsx:1` (65216b), `src/**/*.py` (9 dosya), `experiments/phase_01_dryrun/*`.

---

## 1) Yönetici Özeti — Ne kadar tamamlandı?

| Katman | Durum | Kanıt |
|---|---|---|
| **Faz 0 Protokol Kilidi** | ✅ **Tamam** | `config/experiment.yaml:1` `protocol_version: 1.0-locked` 2026-08-27, `PROTOCOL.md:1` 20 madde tek kaynak |
| **Faz 1 Dry-Run** | ✅ **Teknik olarak tamam, evrakta 8/9** | `STARTED_AT:1` <tarih> → <tarih> <süre> `>7`. `docker ps` 3 container `Up <süre>`. DB <N> trade (<A> açık/<K> kapalı), heartbeat `RUNNING`. Tek fail: `scripts/verify_dryrun.py:21` `dry_run_wallet==10` bekler, `config/freqtrade.example.json:7` `100` → 8/9 (`PHASE_LOCK.md:7` 8/9 notu uyumlu) |
| **Faz 2 Walk-Forward** | ⚠️ **İskelet var, hiç koşmadı** | `src/backtest/walk_forward.py:1` + `evaluate.py:1` var, `experiments/phase_02_walkforward/` boş, `Makefile:14` `walkforward` target var ama fold raporu üretilmedi |
| **Faz 3 FreqAI** | ❌ **Yok** | Repo'da `freqai` klasörü/model yok, `PROTOCOL.md:25` öngörülüyor ama kod yok |
| **Faz 4 RL** | ⚠️ **İskelet, yarım** | `src/rl/env.py:1` `TradingEnv` (Discrete 3, reward `log return` sadece kapanışta) + `src/rl/train.py:1` var ama `SAC` branch'i bile `PPO` döndürüyor, `total_timesteps=100_000` hardcode, `experiments/phase_04_rl/` boş |
| **Faz 6-7 X/Haber** | ⚠️ **İzole iskelet, kapalı** | `src/x_layer/scraper.py:7` `X_ENABLED=false` no-op, `signal.py:7` `event_study/IC` var, `isolation.md:1` sözleşme var, canlı veri yok |
| **Faz 10 Paper / 11 Real** | ❌ **Yok** | `scripts/paper_vs_backtest.py:1` stub, `experiments/phase_10_paper/` boş, `trading_mode: spot` `margin_mode: isolated` ama withdraw kapalı |
| **Dashboard** | ✅ **Çalışıyor, tek bug düzeltildi** | `dashboard/backend/app.py:1` 17 endpoint, `frontend/src/App.tsx:1` 12 panel, `docker-compose.yml:14` build-time copy (hot-reload yok) — `open_positions` statik wallet bug'ı `app.py:784` `wallet=starting+realized` ile düzeltildi (önce `<gizlendi>` → şimdi `<gizlendi>`) |
| **Sermaye/Koruma** | ⚠️ **Muhasebe doğru, yürütme yok** | `src/risk/capital.py:1` ≡ `dashboard/backend/capital.py:1` (identical diff), `config/capital.json:1` `core 100`, `protection lock_ratio 0.5 trailing`, `harvest auto_enabled false`. `docs/CAPITAL_PROTECTION_EXECUTION_DESIGN.md:1` sadece tasarım, hiçbir `ccxt.withdraw` yok |

**Net:** Protokol ve altyapı kurulu (%40), gerçek araştırma (WF/FreqAI/RL/X) hiç başlamadı (%0), gözlem katmanı tamam (%95).

---

## 2) Mevcut Sistemde Neler Gerçekten Var?

### 2.1 Çalışan Canlı Sistem
* **Freqtrade** `freqtradeorg/freqtrade:stable` `docker-compose.yml:3` `ai-trader-dryrun` → `trade --config /freqtrade/config.json --strategy BaselineStrategy --dry-run` . `freqtrade/user_data/strategies/BaselineStrategy.py:1` canlı sanal emir basıyor. `DB` `tradesv3.dryrun.sqlite:trades` 37 `orders` ile senkron.
* **Config** `config/freqtrade.example.json:1` `max_open_trades:3` `stake_amount:unlimited` `tradable_balance_ratio:0.99` `dry_run_wallet:100` `VolumePairList TOP 30 min_value 5M` + 5 filtre (`Age/Precision/Price/Spread/Volatility`). `stake_currency USDT` `trading_mode spot`.
* **Dashboard API** `dashboard/backend/app.py:1` FastAPI 17 endpoint (aşağıda). **Güvenlik:** `safe_config():471` sadece whitelist, `LIVE_TRADING:DISABLED` sabit, `freqtrade.live.json` varsa `control:403`, portlar `127.0.0.1:8001/5173` sadece localhost, volume `:ro`, `mode=ro&nolock=1` + `backup` fallback `app.py:488`. Secret sızmaz (tek `secret` kelimesi `capital.py` "secret" içermez).
* **Dashboard Frontend** `App.tsx:1` Vite+Recharts, 4-8sn polling (`usePoll`), 12 panel, Binance WS `wss://stream.binance.com:9443/stream?streams={30x}@trade` 500ms throttle, fallback 1.5sn REST.

### 2.2 Araştırma İskeletleri (Koşmuyor)
* `src/data/metrics.py:1` `sharpe/sortino/max_drawdown/profit_factor` (primary `oos_sharpe_ratio:365`)
* `src/backtest/walk_forward.py:1` `WFConfig 365/90/90/90 expanding:false` `generate_folds` + `overlap_report`
* `src/regime/classifier.py:1` `RegimeConfig SMA50/200 ADX>20 ATR 30d percentile` → `bull/bear/sideways_high_vol` (mekanik, `experiment.yaml:26` kilitli)
* `src/rl/env.py:1` `gymnasium` discrete 3, `window 30`, reward `log(portfolio)` sadece sell'de, mark-to-market yok
* `src/x_layer/*` Playwright stub + `signal.py` IC/event_study

### 2.3 Dosya Haritası (5k+ dosya, node_modules hariç 45)
`config:4` `dashboard/backend:2py` `frontend:4ts` `scripts:3py` `src:9py` `tests:1py` `freqtrade/user_data:1py+DB` `experiments:2` `data:1json` — `Makefile:1` 6 target, `requirements.txt:1` 10 lib (freqtrade, pandas, TA-Lib, gymnasium, SB3, playwright, sklearn).

---

## 3) Faz Faz Durum Tablosu

| Faz (PROTOCOL.md) | Plan | Gerçek | Kalan |
|---|---|---|---|
| **0 — Protokol Kilidi** | `experiment.yaml` `locked_at:2026-08-27` `locked_by` | ✅ Kilitli, 20 madde, `thresholds` `phase_03/05/10` X=0.30 Y=0.60 vb. | — |
| **1 — Dry-Run** | 7 gün kesintisiz, `verify 9/9` | ✅ <süre>, `verify 8/9` (wallet mismatch) | `verify_dryrun.py:21` `wallet==100` düzelt, 9/9 rapor arşivle |
| **2 — WF Backtest** | `freqtrade backtesting --timerange` rolling 365/90/90 | ⚠️ `walk_forward.py` var, hiç `freqtrade backtesting` çıktısı yok, `evaluate.py` `multipletests FDR` var ama `trades.csv` yok | İlk WF koş, `experiments/phase_02_walkforward/trades.csv` üret, `overlap_report` kaydet |
| **3 — FreqAI** | `Market→Features→ML→Signal` `min_sharpe_delta 0.30` | ❌ Kod/yaml yok, `experiment.yaml:31` eşik boşta | FreqAI config + feature set + Train/Val/Test A (2023-07/12) |
| **4 — RL** | Gymnasium PPO/SAC `log return` `budget 50/5/12h` | ⚠️ `env.py` mark-to-market yok, `train.py` SAC→PPO bug, budget okunmuyor | Reward'ı düzelt, `rl_budget` `experiment.yaml:38` kullan, seed loop ekle, `final_test_B` (2024-01/06) ayır |
| **5 — Karşılaştırma** | Baseline vs FreqAI vs RL `FDR` `cohens_d≥0.30` | ❌ Yok | WF pencereleri + seed stability `<0.25` |
| **6-8 — X/Haber Multi-Source** | `X_ENABLED` izole, `IC/precision/recall` `event_study` | ⚠️ `scraper.py` no-op, `signal.py` test edilmedi | `X_ENABLED=true` ile 1dk/5dk/15dk/1h `forward_ret` topla, eşiği geçemezse çıkar (`PROTOCOL 13`) |
| **10 — Paper** | 30 gün canlı paper `paper_vs_backtest.py` gap `0.35` | ❌ Yok | `phase_10` 30 gün başlat, `fee+5bps` `news*2` raporla |
| **11 — Real** | Read✅ Trading✅ Withdraw❌ `min_notional` | ❌ Kilitli, doğru | — |

---

## 4) Sonradan Eklediklerimiz (Planda Olmayan)

* **VolumePairList evren** `config/freqtrade.example.json:20` `TOP 30 min_value 5M refresh 1800` + 5 filtre — `PROTOCOL.md` pazar evrenini tanımlamaz, biz ekledik. Freqtrade default whitelist yerine dinamik keşif.
* **Sermaye/Rezerv Katmanı** `src/risk/capital.py:1` + `config/capital.json:1` + `docs/CAPITAL_PROTECTION_EXECUTION_DESIGN.md:1` — `PROTOCOL.md`'de yok. CORE 100 kilit, `HWM/locked/threshold/reserve` trailing muhasebe. Son düzeltme `tests/test_capital_protection.py:1` `realized==0 → reserve==0` bugfix'i.
* **Dashboard** tümü (`dashboard/README.md:1`) — `PROTOCOL.md` sadece "P&L kaydediliyor" der, biz 12 panel + WS ticker + health + logs + capital + experiment ekledik. `docker-compose.yml:14` 2 servis + `zennet`.
* **Plan dışı `data/capital_state.json` vs `freqtrade/user_data/capital_state.json` ikilisi** — `STATE_PATHS_ALL:39` writable öncelik, drift riski.

---

## 5) Şu Anki Strateji Tam Olarak Ne Yapıyor?

`BaselineStrategy.py:1` — **Faz 2 referans baseline, nihai strateji değil:**

```python
timeframe 5m, can_short False, stoploss -0.10, minimal_roi {"0":0.02}, startup 200
indicators: RSI14, SMA50, SMA200 (talib)
enter_long: RSI < 30 & SMA50 > SMA200  # IntParameter 10-40 default 30
exit_long: RSI > 70                     # 60-90 default 70
```

**Ne demek:** 5dk kapalı mumda RSI aşırı satım + uptrend ise AL, aşırı alım ise SAT. `populate_*` sadece TA-Lib Wilder RSI (fiyat ağırlıksız). `N=30` coin içinde `RSI<30` nadir → seyrek trade (8 günde 19). `unlimited` stake → `≈ (wallet_realized*0.99)/3 ≈ 34.1 USDT` per trade, 3 eşzamanlı max. Slippage/fee `0.001` simüle, gerçek fill `price_side same`.

**Ne yapmıyor:** FreqAI/RL yok, haber yok, feature engineering yok, short yok, trailing SL yok, kaldıraç yok.

---

## 6) Dashboard Ne Gösteriyor?

`app.py` 17 endpoint → `App.tsx` 12 kart (poll 4-8sn):

| Kart | Kaynak | Hesap |
|---|---|---|
| **Header** | `status:570` `docker inspect` + `heartbeat:533` `docker logs --tail 200` + `uptime` | `bot_status RUNNING/STOPPED` |
| **Experiment** | `experiment:906` `STARTED_AT` + `PHASE_LOCK.md` + `verify_dryrun.py` | `<süre>` `LOCKED` |
| **Portfolio** | `portfolio:603` `SUM(close_profit_abs)` + Binance `ticker/price` | `starting <gizlendi>` `realized <gizlendi>` `unrealized <gizlendi>` `current <gizlendi>` |
| **Açık Pozisyonlar** | `open_positions:694` `trades is_open=1` + bulk `ticker/24hr` | `invested <gizlendi>` `value <gizlendi>` `unreal <gizlendi>` `free <gizlendi>` *(düzeltildi)* |
| **Yatırılan / Serbest / Değer** | aynı `summary` | KPI |
| **CORE/REZERV** | `capital:647` `capital.py:308` `compute_capital_view` | `CORE 100 🔒` `threshold <gizlendi>` `locked <gizlendi>` `reserve <gizlendi>` `equity <gizlendi>` `HWM <gizlendi>` |
| **Açık Pozisyon Kartları** | `open_positions:413` `value/PL/duration` | per-pair |
| **Live Market** | `market:363` `get_universe()+get_pair_data` paralel 12 thread + `569` WS `@trade` | 30 coin `price/RSI/SMA/signal AL/SAT/BEKLE` `24h%` klines 72 |
| **Çoklu Timeframe** | `market/timeframes:429` | `1S/1G/1H/1Ay/1Y` görsel |
| **Performance** | `performance:855` `equity/drawdown` `win <gizlendi>` `PF <gizlendi>` `Sharpe <gizlendi>` | (kısa örneklem) |
| **Health / Logs / Alerts** | `health:942` `psutil` `logs:983` `docker logs` `alerts:1008` | disk/mem/cpu |

**Güvenlik:** `LIVE_TRADING:DISABLED` `freqtrade.live.json` 403, volume `:ro`, `mode=ro`, secret expose yok.

---

## 7) Sermaye / Rezerv Sisteminin Durumu

* **Mevcut:** Sadece **muhasebe**. `src/risk/capital.py:257` `compute_capital_view()` `equity=CORE+realized+unrealized` bulk ticker, `HWM=max(HWM,equity)`, `locked=(HWM-CORE)*0.5` trailing, `threshold=CORE+locked`, `reserve=min(locked, realized*0.5)` (bugfix `tests/test_capital_protection.py:7` — önce `reserve=locked` iken `realized 0` olsa bile `0.29` gösteriyordu, şimdi `0`).
* **Durum 2026-09-04:** `data/capital_state.json:1` `core 100` `HWM <gizlendi>` `locked <gizlendi>` `threshold <gizlendi>` `reserve <gizlendi>` `realized <gizlendi>` (DB realized ile drift — `freqtrade/user_data/capital_state.json` eski `<gizlendi>`). `protection_status: protected` `distance <gizlendi>`.
* **Harvest:** `auto_enabled false` `ratio 0.5 period daily threshold 1.0` — sadece manuel `POST /capital/harvest?force=true`. Hiç `withdraw/transfer` yok (`grep withdraw` boş) — `CAPITAL_PROTECTION_EXECUTION_DESIGN.md:4` sanal rezerv (4.1) vs sub-account (4.2) tasarımı duruyor.
* **Risk:** `STATE_PATHS_ALL:39` 4 yol, writable `data/` öncelik ama `freqtrade/user_data/capital_state.json` senkron değil → drift.

---

## 8) Planlanan AI / FreqAI / RL / X Katmanları

* **FreqAI (Faz 3)** `PROTOCOL.md:23` — `experiment.yaml:31` `min_sharpe_delta 0.30` `min_wf_win_rate 0.60` `max_dd 0.20` `seed_std<0.25` geçmeden Faz 5 yok. Henüz feature/label yok.
* **RL (Faz 4)** `PROTOCOL.md:26` — `env.py:16` `Discrete(3)` hold/buy/sell, window 30, fee 0.001, reward sadece `log sell` (mark-to-market yok), `train.py:28` PPO `100_000` step hardcode, `budget 50/5/12h` kullanılmıyor (`experiment.yaml:38`). Ayrı final test B (2024-01/06) korunmalı.
* **X (Faz 6-7)** `PROTOCOL.md:28` — `scraper.py:11` Playwright `X_ENABLED=false`, `isolation.md:3` ayrı container/IP, `signal.py:14` `ret_1/5/15/60m` `IC spearman`. Önce predictive power (IC>?, precision) yoksa trader'a bağlanmayacak (`PROTOCOL 13`).
* **Multi-Source (Faz 8)** — sadece kanıtlı kaynaklar `Decision Layer` → `BUY/HOLD/SELL` (LLM direkt alım yok `Asset/Event/Sentiment/Novelty/Confidence`).
* **Nautilus (Faz 9)** — ihtiyaç olursa.

---

## 9) Kilitlediğimiz Deney Kurallarına Uyum (20 Madde)

| # | Kural | Uyum | Not |
|---|---|---|---|
| 1 | Eşikler önce | ✅ | `experiment.yaml:31` `phase_03/05/10` kilitli 2026-08-27, sonuç görülmedi |
| 2 | Final Test tuning'e kapalı | ✅ | `splits:4` A/B/C ayrı, hiç `final_test_A` kullanılmadı |
| 3 | Bir fazın Final Test'i başkasında kullanılamaz | ✅ | `walk_forward.py` `final_test_A` `2023-07/12`, RL `B` `2024-01/06` örtüşmez |
| 4 | Final Test kullanılırsa yeni final gerekir | ✅ | Henüz hiç final koşulmadı |
| 5-6 | Primary `oos_sharpe_ratio`, secondary ile ilan yok | ✅ | `primary_metric: sharpe` `metrics.py:5` tek |
| 7 | Çoklu karşılaştırma düzeltmesi | ✅ | `evaluate.py:12` `multipletests fdr_bh` hazır |
| 8 | WF foldları bağımsız sayılmaz | ✅ | `walk_forward.py:22` `overlap_report` + `note` var |
| 9 | Rejim hindsight değil, mekanik kilitli | ✅ | `classifier.py:12` `RegimeConfig` `experiment.yaml:26` kilitli, sadece geçmiş |
| 10 | RL tuning sadece Train+Val | ✅ | `train.py` `df_path` Train+Val, Final B'ye dokunmuyor (henüz) |
| 11 | RL ilk failde elenmez | ✅ | `rl_budget max 50/5/12h` var, `train.py` henüz budget'i zorlamıyor ama niyet korunuyor |
| 12 | Backtest slippage gerçek değil | ✅ | `experiment.yaml:35` `5bps base news*2` `report_news_spike_gap true` |
| 13-15 | X predictive olmadan bağlanmaz, izole, kapanırsa sistem durmaz | ✅ | `X_ENABLED=false` no-op, `isolation.md:3` sözleşme |
| 16 | Her model baseline'a kıyas | ✅ | `BaselineStrategy` `minimal_roi 0.02` referans donduruldu |
| 17 | Sıra Backtest→WF→Paper→Real | ✅ | `PHASE_LOCK.md:6` Faz1 bitmeden Faz2/3 dokunulmaz kuralı kondu |
| 18 | Anlamlı fark yok ≠ edge yok, Type II rapor | ✅ | `evaluate.py:16` `cohens_d` + `power_target 0.80` var |
| 19 | Gerçek para en son | ✅ | `trading_mode spot` `dry_run true` `LIVE_TRADING DISABLED` |
| 20 | Sonuç görülünce kural değişmez | ✅ | `verify_dryrun.py` wallet mismatch hariç hiçbir threshold değişmedi |

**Tek sapma:** `PHASE_LOCK.md:6` *Faz1 7 gün dolmadan Faz2/3 koduna dokunulmaz* — `src/backtest/*` ve `src/rl/*` dosyaları **kilit tarihinden önce** iskelet olarak oluşturuldu (`locked_at 2026-08-27` sonrası `git` yok, mtime kontrol edilemiyor). Faz 1 koşarken bu dosyalara **yazma yapılmadı** ( `docker ps` sadece Freqtrade), bu yüzden fiili ihlal yok; ama prensip için Faz 2 klasörleri boş bırakılmalıydı. Bir sonraki deneyde `git tag` ile kanıtlanmalı.

---

## 10) Verimlilik ve Sistem Analizi

**Canlı <süre> (<tarih aralığı>)**
* `<N> trade` `<K> kapalı` `<A> açık` `orders <N>` → `<N> trade/gün`. `stake≈<tutar>` → turnover `<oran>`.
* `realized <gizlendi>` return <süre>de (annualized hesabı örneklem küçük). `avg_win <gizlendi>` `avg_loss <gizlendi>` `win <gizlendi>` `PF <gizlendi>` `max_dd <gizlendi>` `Sharpe <gizlendi>` `Sortino <gizlendi>` → **Sharpe anualization 365 ile şişmiş**, gerçek `periods_per_year` 5m için `105120` değil, `365` günlük return varsayıyor → abartı. `metrics.py:7` `periods_per_year=365` kripto için gün bazlı doğru ama trade return'ü günlük değil → düzeltmeli.
* **Verimsiz:** `N=30` evrende `RSI<30` nadir, çoğu `BEKLE`, capital atıl değil (`<N>` dolu) ama sinyal seyrek → `unreal <gizlendi>` drawdown `invested <gizlendi>` üzerinde.

**Sistem yükü:** `app.py:359` 12 thread market tarama, `Bruno` `get_timeframe_stats` 5 interval `limit 42/365` → Binance rate-limit 1200/min içinde, `heartbeat 5sn` hafif. `psutil` disk/mem `warn>85%`.

---

## 11) Eksik / Hatalı / Riskli Yerler

1. **Kritik: `verify_dryrun.py:21` `wallet==10` vs `config:7` `100` → Faz1 8/9 fail**. README ` $10 sanal bakiye` der, config `100`. Ya config `10` yapılacak ya verify `100` olacak — şu an resmi çıkış kriteri sağlanmıyor.
2. **Dashboard build-time copy** `docker-compose.yml:19` `:ro` mount değil, `app.py` hot-reload yok — her düzeltmede `up --build` gerekir (bugün yapıldı).
3. **Capital state drift** `data/capital_state.json:6` `HWM <gizlendi>` vs `freqtrade/user_data/capital_state.json:6` `<gizlendi>` — çift yazım `STATE_PATHS_ALL:39` `save_state:125` 4 yola atomic write ama `freqtrade` mount `ro` değil `rw`? Aslında `freqtrade/user_data` RO, yazılamayınca `data/` ve `/tmp`'ye düşüyor → drift normal ama teyit edilmeli.
4. **Mock unrealized şişmesi** `app.py:604` `binance_price()` bulk ticker fallback tekil `ticker/price` — rate-limit'te `None` dönerse `value=stake` → equity yanlış. Gerçek `unrealized <gizlendi>` doğru ama `portfolio.py` `mock <N>` testinde `<tutar>` gördük.
5. **RL bug** `train.py:22` `if algo=="SAC": from SAC ... model=PPO` → SAC hiç eğitilmiyor.
6. **WF `expanding false` sabit** `walk_forward.py:9` — anchored test yok, `step_days 90` `test 90` örtüşme raporu var ama istatistiksel bağımsızlık düzeltmesi yok.
7. **Version kontrol yok** `git` repo değil (çalışma klasöründe `.git` yok) → `locked` dosyaların değişimi kanıtlanamıyor, `PROTOCOL 20` için risk.
8. **Slippage raporu yok** `paper_vs_backtest.py` stub, `CAPITAL_PROTECTION_EXECUTION_DESIGN.md:5` sadece tasarım, yürütme `4.1 virtual` bile implement değil → paper/live gap bilinmiyor.

---

## 12) Sonraki Aşamalarda Neler Yapılabilir (Öneri Sırası)

**Faz 1 kapanış (1 gün):**
* `verify_dryrun.py:21` `100` yap veya config `10` yap → `9/9` arşivle, `STARTED_AT` 8 günü tag'le, `data/capital_state.json` snapshot al.

**Faz 2 (1-2 hafta):**
* `scripts/download_data.py:1` `400 gün` `BTC/ETH + TOP30` indir → `walk_forward.py --start 2020-01-01 --end 2023-12-31` 13 fold koş → `evaluate.py` `sharpe/sortino/dd/pf` + `fdr` tablo. `regime/classifier` `bull/bear/high_vol` kırılımlı raporla.
* `experiment.yaml` `final_test_A` 2023-07/12'yi sakla, dokunma.

**Faz 3 (2-3 hafta):**
* FreqAI feature set (price/volume/RSI/SMA/ATR) + `5-fold` + `seed 5` `X delta 0.30` test et, `WF win 60%` bak.

**Faz 4 (3-4 hafta):**
* `env.py` mark-to-market reward ekle, `train.py` `SAC` düzelt, `rl_budget 50/5/12h` loop'u implement et, `final_test_B` 2024-01/06.

**Faz 6-7 (paralel):**
* `X_ENABLED=true` tek alt hesap, `scraper.py` selector'ları test et, `signal.py` `1/5/15/60m` `IC` topla, `α=0.05 FDR` geçemezse çıkar.

**Faz 10:**
* `paper_vs_backtest.py` gerçek gap: `sharpe gap 0.35`, `spread/latency` logla.

---

**Ekler:** `README.md:1`, `PROTOCOL.md:1`, `experiment.yaml:1`, `BaselineStrategy.py:14`, `app.py:359` market tarama `30`, `capital.py:308` HWM, `verify_dryrun.py:44` 8/9.
